# robots.txt for Today in History Archive
# Reference: https://developers.google.com/search/docs/crawling-indexing/robots

# Allow all crawlers to access all content
User-agent: *
Allow: /

# Block search and pagination URLs from indexing (avoid duplicate content)
Disallow: /pagefind/
Disallow: /search?

# Crawl delay for politeness
Crawl-delay: 1

# Sitemap location (auto-resolved to current site baseURL)
Sitemap: https://history-it.ai-term-hub.com/sitemap.xml
